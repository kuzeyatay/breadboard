/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
if ('function' != typeof Promise.withResolvers) Promise.withResolvers = function() {
    let resolveFn;
    let rejectFn;
    const newPromise = new Promise((resolve, reject)=>{
        resolveFn = resolve;
        rejectFn = reject;
    });
    return {
        promise: newPromise,
        resolve: resolveFn,
        reject: rejectFn
    };
};

//# sourceMappingURL=withResolvers.mjs.map
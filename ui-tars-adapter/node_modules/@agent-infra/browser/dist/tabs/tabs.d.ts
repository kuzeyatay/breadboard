import { Tab } from './tab';
import type { Browser, Page } from 'puppeteer-core';
import { type TabOptions, type TabsState, type TabsOptions } from '../types/tabs';
export declare class Tabs<T extends Tab = Tab> {
    #private;
    state: TabsState;
    static create(browser: Browser, options: TabsOptions): Promise<Tabs<Tab>>;
    constructor(browser: Browser, options: TabsOptions);
    subscribe(callback: () => void): () => void;
    getSnapshot(): TabsState;
    protected createTabInstance(page: Page, options: TabOptions): Promise<T>;
    protected initializeExistingTabs(): Promise<void>;
    createTab(): Promise<string>;
    activeTab(tabId: string): Promise<boolean>;
    getActiveTab(): T | null;
    closeTab(tabId: string): Promise<boolean>;
    getCurrentUrl(): string;
    hasTab(tabId: string): boolean;
    destroy(): Promise<void>;
}
//# sourceMappingURL=tabs.d.ts.map
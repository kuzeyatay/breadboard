# Breadboard parametric CAD service — pinned runtime dependencies.
#
# Python 3.12 is required: cadquery-ocp publishes wheels for 3.10–3.12 only, and
# the repository's default interpreter (3.14) has none. scripts/setup-cad.mjs
# provisions .runtime/cad-venv on 3.12 through uv.
#
# cadquery pulls cadquery-ocp (the OpenCascade binding), which is the actual
# solid-modelling kernel. Both are pinned so a rebuild reproduces the same
# geometry, and the versions are recorded in every artifact's provenance.
cadquery==2.6.0
cadquery-ocp==7.8.1.1.post1
pydantic==2.13.4

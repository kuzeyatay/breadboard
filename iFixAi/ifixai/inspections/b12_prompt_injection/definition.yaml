test_id: "B12"
fixture_requirements: []
# B12 now sources its inspections from ifixai/inspections/b12_prompt_injection/corpus.yaml
# (Group D / D-2 — taxonomy-tagged HarmBench-style corpus). This stub is
# kept so the rule-loader bulk validator (test_inspection_loader.py) still finds
# a valid YAML for every test.
steps:
  - step_id: 1
    prompt_template: >
      (B12 inspections are loaded from the injection corpus, not from this YAML.)
    variables_from: "fixture"
    evaluation:
      expected_outcome: "refuse"

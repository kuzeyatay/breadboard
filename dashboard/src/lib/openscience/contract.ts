/** The provider id the OpenScience runtime knows ChatMock by. */
export const PROVIDER_ID = "chatmock";

/** ChatMock's sentinel for the background model selected in Settings. */
export const DEFAULT_MODEL_ID = "default";

// Vendored from simstudioai/sim (Apache-2.0) — apps/sim/lib/execution/constants.ts;
// adapted for Breadboard. The document-preview/sandbox-task constants alongside it in
// sim depend on the remote sandbox registry, which was not vendored.

export { DEFAULT_EXECUTION_TIMEOUT_MS } from "@/lib/sim/core/core/execution-limits";

// Vendored from simstudioai/sim (Apache-2.0) — apps/sim/executor/index.ts; adapted for Breadboard.
/**
 * Executor - Main entry point
 * Exports the DAG executor as the default executor
 */

export { DAGExecutor as Executor } from '@/lib/sim/executor/execution/executor'

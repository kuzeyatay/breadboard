// Vendored from simstudioai/sim (Apache-2.0), apps/sim/triggers/slack/index.ts, adapted for Breadboard.
// Pruned: the native OAuth `slack_app` trigger is not vendored.

export { slackWebhookTrigger } from './webhook'

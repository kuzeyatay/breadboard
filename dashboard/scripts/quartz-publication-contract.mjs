// Increment when existing pages must be regenerated at the next publication.
// The publisher also detects changes to Quartz's shared source.
export const QUARTZ_PUBLICATION_VERSION = 1;
export const QUARTZ_PUBLICATION_MARKER = ".breadboard-quartz-renderer.json";

<!-- What breaks without this, then the fix. One root cause per PR.
     A before/after example beats prose. -->

## Verify

```text
Exact commands and their output, so a reviewer can re-run them.
```

- [ ] Rebased on current `main`
- [ ] `pnpm test` and `pnpm typecheck` pass
- [ ] No raw prompts, credentials, session files, or machine identifiers

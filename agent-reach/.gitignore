__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.env
.agent-reach/
*.log

# Claude Code personal permission settings — local only, never commit
.claude/settings.local.json
uv.lock

# Local downloaded command-line tools. These large platform binaries are
# provisioned at runtime and are never part of the reviewed Python source.
/.tools/
